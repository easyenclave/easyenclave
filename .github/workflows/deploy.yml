name: deploy

on:
  push:
    branches: [main]

jobs:
  release:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - name: Build release artifacts
        run: |
          cargo build --release --workspace
          image/build.sh
      - name: Placeholder production deploy
        run: |
          echo "TODO: production rollout to GCP using PRODUCTION_GCP_* secrets"
