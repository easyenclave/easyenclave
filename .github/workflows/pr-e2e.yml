name: pr-e2e

on:
  pull_request:
    branches: [main]

jobs:
  e2e:
    runs-on: self-hosted
    env:
      PR_NUMBER: ${{ github.event.pull_request.number }}
      GCP_PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
      GCP_SERVICE_ACCOUNT_KEY: ${{ secrets.GCP_SERVICE_ACCOUNT_KEY }}
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_ZONE_ID: ${{ secrets.CLOUDFLARE_ZONE_ID }}
      ITA_API_KEY: ${{ secrets.ITA_API_KEY }}
      ITA_APPRAISAL_URL: ${{ vars.ITA_APPRAISAL_URL }}
      EE_GCP_SOURCE_IMAGE_FAMILY: ${{ vars.EE_GCP_SOURCE_IMAGE_FAMILY }}
      EE_GCP_SOURCE_IMAGE_PROJECT: ${{ vars.EE_GCP_SOURCE_IMAGE_PROJECT }}
      GCP_ZONE: us-central1-a
      EASYENCLAVE_DOMAIN: easyenclave.com
      CP_RUNTIME_MODE: guest
      CP_RUNTIME_FALLBACK_LOCAL: true
    steps:
      - uses: actions/checkout@v4
      - name: Run PR e2e checks
        run: scripts/pr_e2e.sh
