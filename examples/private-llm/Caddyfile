:8080 {
	route {
		@health path /health
		handle @health {
			header Content-Type application/json
			respond "{\"ok\":true}" 200
		}

		@api path /api/*
		handle @api {
			reverse_proxy private-llm:11434
		}

		root * /srv
		try_files {path} /index.html
		file_server
	}
}
