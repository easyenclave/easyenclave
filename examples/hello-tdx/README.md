# Hello TDX

Minimal example showing a service running in a TDX enclave.

## Deploy

Deployed automatically by the [Deploy Examples](../../.github/workflows/deploy-examples.yml) workflow after CI passes on `main`.
