# EFI System Partition — holds the UKI (Unified Kernel Image)
# The UKI bundles kernel + initrd + cmdline (with roothash).
# We extract the boot artifacts from the UKI at build time using objcopy.
[Partition]
Type=esp
Format=vfat
CopyFiles=/efi:/
CopyFiles=/boot:/
SizeMinBytes=256M
SizeMaxBytes=512M
