# EasyEnclave TDX VM fstab
#
# Root is mounted read-only via dm-verity (handled by initrd).
# Writable areas are tmpfs. No physical data or config disks.

# Writable tmpfs mounts
tmpfs   /tmp        tmpfs   defaults,nosuid,nodev,size=512M    0 0
tmpfs   /var/tmp    tmpfs   defaults,nosuid,nodev,size=256M    0 0
tmpfs   /var/log    tmpfs   defaults,nosuid,nodev,size=256M    0 0
tmpfs   /var/lib    tmpfs   defaults,nosuid,nodev,size=512M    0 0
# /data is mounted by data-disk.service (virtio disk or tmpfs fallback)
