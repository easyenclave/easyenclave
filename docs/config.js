// Configuration for EasyEnclave Web GUI
const CONFIG = {
    // API base URL - backend hosted at app.easyenclave.com
    API_BASE: 'https://app.easyenclave.com/api',

    // Refresh interval in milliseconds
    REFRESH_INTERVAL: 30000,
};
