# Build App Guide (Moved)

The build flow is now combined with deployment in one runbook:

- [`docs/runbooks/deploy-app.md`](../runbooks/deploy-app.md)

Use that page for the full end-to-end path: start from examples, build/test locally, register app, publish version, and deploy to agents.
